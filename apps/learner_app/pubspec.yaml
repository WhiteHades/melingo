name: learner_app
description: Melangua learner app
publish_to: none
version: 0.1.0+1

environment:
  sdk: ">=3.4.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter
  cryptography: ^2.7.0
  flutter_secure_storage: ^10.0.0
  intl: ^0.20.2
  flutter_riverpod: ^3.3.1
  go_router: ^17.2.0
  http: ^1.5.0
  shared_preferences: ^2.5.3
  firebase_core: ^4.6.0
  firebase_auth: ^6.3.0
  cloud_firestore: ^6.2.0
  cloud_functions: ^6.1.0
  firebase_storage: ^13.2.0
  firebase_app_check: ^0.4.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
  http_mock_adapter: ^0.6.1

flutter:
  generate: true
  uses-material-design: true
